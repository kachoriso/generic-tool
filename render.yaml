services:
  - type: web
    name: generic-tool
    env: node
    runtime: node
    buildCommand: ./build.sh
    startCommand: npx serve -s dist -p $PORT
    plan: free
    envVars:
      - key: NODE_VERSION
        value: "22" 