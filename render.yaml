services:
  - type: web
    name: generic-tool
    env: node
    buildCommand: pwd && ls -la && npm ci && npm run build && ls -la dist/ || (echo "Build failed, checking directory:" && ls -la)
    startCommand: ls -la dist/ && npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production 